#!/usr/bin/env bash
# The first line is called a shebang and the absolute path to your preferred interpreter

# What you type on the Terminal can also by typed here and executed. Try executing this script
echo "Hello, world!"

# You cannot execute this file because you don't have permission to do so
# Change permissions with `chmod +x hello.sh` and verify with `ls -l`
# Now try and execute this file again
